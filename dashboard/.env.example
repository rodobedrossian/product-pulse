# Local: matches Vite proxy / or API port. Production: full public API URL (https://…, no trailing slash).
VITE_API_URL=http://localhost:3001

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Public dashboard origin (invite links, /join/…). Local default is fine for dev.
VITE_APP_URL=http://localhost:5173
